# Copy this file to .env (gitignored) and fill in your own values.
#
# Create a free Strava API app at https://www.strava.com/settings/api
# (no approval process — it's instant). Set "Authorization Callback Domain"
# to "localhost". Then copy the Client ID and Client Secret from that page.
VITE_STRAVA_CLIENT_ID=
VITE_STRAVA_CLIENT_SECRET=
